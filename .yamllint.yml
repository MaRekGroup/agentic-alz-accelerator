---
extends: default

rules:
  line-length:
    max: 250
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1
  comments-indentation: disable
  document-start: disable
  new-lines:
    type: platform
